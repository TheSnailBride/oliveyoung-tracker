services:
  redis:
    image: redis:7-alpine
    container_name: oliveyoung-tracker-redis
    ports:
      - "127.0.0.1:${REDIS_PORT:-16379}:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data

volumes:
  redis-data:
